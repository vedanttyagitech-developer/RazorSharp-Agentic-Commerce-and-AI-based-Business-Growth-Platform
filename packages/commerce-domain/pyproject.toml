[project]
name = "commerce-domain"
version = "0.1.0"
description = "Canonical money, JCS canonicalization and checkout hashing. No I/O, no model calls."
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/commerce_domain"]
