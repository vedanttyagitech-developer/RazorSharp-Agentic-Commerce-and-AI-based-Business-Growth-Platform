'use client';
import {useState} from 'react';
import {Dialog,DialogContent,DialogHeader,DialogTitle,DialogDescription} from './ui/dialog';
import {products,money} from '@/lib/demo';
import {Columns3} from 'lucide-react';
export function ProductComparison(){const [open,setOpen]=useState(false);const [ids,setIds]=useState([products[0].id,products[4].id]);const selected=ids.map(id=>products.find(p=>p.id===id)!);return <><button className="comparison-open" onClick={()=>setOpen(true)}><Columns3 size={15}/> Compare products</button><Dialog open={open} onOpenChange={setOpen}><DialogContent className="concept-dialog comparison-sheet"><DialogHeader><DialogTitle>A clearer choice.</DialogTitle><DialogDescription>Compare recorded sample attributes. Pack prices are not normalized unit prices.</DialogDescription></DialogHeader><div className="comparison-columns">{selected.map((p,i)=><section key={i}><select aria-label={`Comparison product ${i+1}`} value={ids[i]} onChange={e=>setIds(old=>old.map((id,j)=>j===i?e.target.value:id))}>{products.map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</select><img src={p.image} alt={p.name}/><h3>{p.name}</h3><dl><dt>Pack price</dt><dd>{money(p.price)}</dd><dt>Pack description</dt><dd>{p.unit}</dd><dt>Category</dt><dd>{p.category}</dd><dt>Sample stock</dt><dd>{p.stock} units</dd></dl></section>)}</div><p className="muted">Ingredients, dietary suitability and quality ratings are not available in this sample. Ask your copilot for details when verified product data is connected.</p></DialogContent></Dialog></>}
